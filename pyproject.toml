[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "eco-fin-ml"
version = "0.1.0"
description = "Reproducible ML/econometrics research pipeline for finance and banking"
readme = "README.md"
requires-python = ">=3.9"
